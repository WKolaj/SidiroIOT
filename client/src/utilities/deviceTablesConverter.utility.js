export const converter = (data) => {
  console.log(data)
    const entries = Object.entries(data)
    for (const [type, properties] of entries) {
      
      switch(type) {
        case 'MBSwappedFloat':
        case 'MBSwappedUInt32':

      }
    }
    
}

const MBDeviceTemplate = (data) => {
  return 
}